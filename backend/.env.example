# --- Option A: easiest for learning (default) ---
DATABASE_URL=sqlite:///./creditpilot.db

# --- Option B: PostgreSQL via Docker (when Docker Desktop is running) ---
# DATABASE_URL=postgresql+psycopg://creditpilot:creditpilot@localhost:5432/creditpilot

SECRET_KEY=change-me-to-a-long-random-string
ACCESS_TOKEN_EXPIRE_MINUTES=60
